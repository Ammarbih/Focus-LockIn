import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.FocusLockin.FocusLockin',
  appName: 'Focus Lockin',
  webDir: 'build'
};

export default config;
